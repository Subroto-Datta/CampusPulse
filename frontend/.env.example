# ─── API URL ──────────────────────────────────────
# Local dev: leave as /api (Vite proxy handles it)
# Production (Amplify): set to your API Gateway URL
#   e.g. https://abc123.execute-api.ap-south-1.amazonaws.com/api
VITE_API_URL=/api
